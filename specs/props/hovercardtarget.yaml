children:
  originalType: React.ReactNode
  group: General
  caption: Children
  description: Target element
eventPropsWrapperName:
  originalType: string
  group: General
  caption: Event Props Wrapper Name
  description: Key of the prop used to pass event listeners, by default event
    listeners are passed directly to component
popupType:
  originalType: string
  group: General
  caption: Popup Type
  description: Popup accessible type
  default: "'dialog'"
refProp:
  originalType: string
  group: General
  caption: Ref Prop
  description: Key of the prop that should be used to access element ref
