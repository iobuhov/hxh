import { BackgroundImagePreviewProps } from "../typings/BackgroundImageProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: BackgroundImagePreviewProps, schema: Properties): Properties {
    return schema;
}
