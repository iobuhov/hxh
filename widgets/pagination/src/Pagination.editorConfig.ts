import { PaginationPreviewProps } from "../typings/PaginationProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: PaginationPreviewProps, schema: Properties): Properties {
    return schema;
}
