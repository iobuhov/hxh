import { PillPreviewProps } from "../typings/PillProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: PillPreviewProps, schema: Properties): Properties {
    return schema;
}
