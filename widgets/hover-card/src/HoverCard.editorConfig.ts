import { HoverCardPreviewProps } from "../typings/HoverCardProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: HoverCardPreviewProps, schema: Properties): Properties {
    return schema;
}
