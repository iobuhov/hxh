import { SliderPreviewProps } from "../typings/SliderProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: SliderPreviewProps, schema: Properties): Properties {
    return schema;
}
