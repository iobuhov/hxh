import { TimelinePreviewProps } from "../typings/TimelineProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: TimelinePreviewProps, schema: Properties): Properties {
    return schema;
}
