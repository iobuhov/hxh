import { MarkPreviewProps } from "../typings/MarkProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: MarkPreviewProps, schema: Properties): Properties {
    return schema;
}
