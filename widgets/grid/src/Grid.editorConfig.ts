import { GridPreviewProps } from "../typings/GridProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: GridPreviewProps, schema: Properties): Properties {
    return schema;
}
