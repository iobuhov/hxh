import { ContainerPreviewProps } from "../typings/ContainerProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: ContainerPreviewProps, schema: Properties): Properties {
    return schema;
}
