import { CenterPreviewProps } from "../typings/CenterProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: CenterPreviewProps, schema: Properties): Properties {
    return schema;
}
