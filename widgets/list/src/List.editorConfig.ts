import { ListPreviewProps } from "../typings/ListProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: ListPreviewProps, schema: Properties): Properties {
    return schema;
}
