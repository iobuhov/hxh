import { RangeSliderPreviewProps } from "../typings/RangeSliderProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: RangeSliderPreviewProps, schema: Properties): Properties {
    return schema;
}
