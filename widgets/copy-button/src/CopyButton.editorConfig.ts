import { CopyButtonPreviewProps } from "../typings/CopyButtonProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: CopyButtonPreviewProps, schema: Properties): Properties {
    return schema;
}
