import { BurgerPreviewProps } from "../typings/BurgerProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: BurgerPreviewProps, schema: Properties): Properties {
    return schema;
}
