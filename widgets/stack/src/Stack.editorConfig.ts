import { StackPreviewProps } from "../typings/StackProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: StackPreviewProps, schema: Properties): Properties {
    return schema;
}
