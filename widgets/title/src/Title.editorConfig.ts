import { TitlePreviewProps } from "../typings/TitleProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: TitlePreviewProps, schema: Properties): Properties {
    return schema;
}
