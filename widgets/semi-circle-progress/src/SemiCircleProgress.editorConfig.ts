import { SemiCircleProgressPreviewProps } from "../typings/SemiCircleProgressProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: SemiCircleProgressPreviewProps, schema: Properties): Properties {
    return schema;
}
