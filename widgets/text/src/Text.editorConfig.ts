import { TextPreviewProps } from "../typings/TextProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: TextPreviewProps, schema: Properties): Properties {
    return schema;
}
