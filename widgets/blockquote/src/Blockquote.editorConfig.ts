import { BlockquotePreviewProps } from "../typings/BlockquoteProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: BlockquotePreviewProps, schema: Properties): Properties {
    return schema;
}
