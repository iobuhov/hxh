import { ChipPreviewProps } from "../typings/ChipProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: ChipPreviewProps, schema: Properties): Properties {
    return schema;
}
