import { SparklinePreviewProps } from "../typings/SparklineProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: SparklinePreviewProps, schema: Properties): Properties {
    return schema;
}
