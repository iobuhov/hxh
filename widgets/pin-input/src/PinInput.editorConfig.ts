import { PinInputPreviewProps } from "../typings/PinInputProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: PinInputPreviewProps, schema: Properties): Properties {
    return schema;
}
