import { FlexPreviewProps } from "../typings/FlexProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: FlexPreviewProps, schema: Properties): Properties {
    return schema;
}
