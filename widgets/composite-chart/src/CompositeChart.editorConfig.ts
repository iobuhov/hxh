import { CompositeChartPreviewProps } from "../typings/CompositeChartProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: CompositeChartPreviewProps, schema: Properties): Properties {
    return schema;
}
