import { RatingPreviewProps } from "../typings/RatingProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: RatingPreviewProps, schema: Properties): Properties {
    return schema;
}
