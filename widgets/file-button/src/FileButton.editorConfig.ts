import { FileButtonPreviewProps } from "../typings/FileButtonProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: FileButtonPreviewProps, schema: Properties): Properties {
    return schema;
}
