import { PopoverPreviewProps } from "../typings/PopoverProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: PopoverPreviewProps, schema: Properties): Properties {
    return schema;
}
