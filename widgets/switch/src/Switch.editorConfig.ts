import { SwitchPreviewProps } from "../typings/SwitchProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: SwitchPreviewProps, schema: Properties): Properties {
    return schema;
}
