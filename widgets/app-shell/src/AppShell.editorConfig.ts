import { AppShellPreviewProps } from "../typings/AppShellProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: AppShellPreviewProps, schema: Properties): Properties {
    return schema;
}
