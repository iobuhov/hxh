import { NavTogglePreviewProps } from "../typings/NavToggleProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: NavTogglePreviewProps, schema: Properties): Properties {
    return schema;
}
