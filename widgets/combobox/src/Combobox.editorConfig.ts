import { ComboboxPreviewProps } from "../typings/ComboboxProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: ComboboxPreviewProps, schema: Properties): Properties {
    return schema;
}
