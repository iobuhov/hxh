import { LineChartPreviewProps } from "../typings/LineChartProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: LineChartPreviewProps, schema: Properties): Properties {
    return schema;
}
