import { GroupPreviewProps } from "../typings/GroupProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: GroupPreviewProps, schema: Properties): Properties {
    return schema;
}
