import { PasswordInputPreviewProps } from "../typings/PasswordInputProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: PasswordInputPreviewProps, schema: Properties): Properties {
    return schema;
}
