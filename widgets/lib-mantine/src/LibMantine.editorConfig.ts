import { LibMantinePreviewProps } from "../typings/LibMantineProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: LibMantinePreviewProps, schema: Properties): Properties {
    return schema;
}
