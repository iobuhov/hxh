import { TooltipPreviewProps } from "../typings/TooltipProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: TooltipPreviewProps, schema: Properties): Properties {
    return schema;
}
