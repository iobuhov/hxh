import { StepperPreviewProps } from "../typings/StepperProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: StepperPreviewProps, schema: Properties): Properties {
    return schema;
}
