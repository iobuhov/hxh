import { NumberInputPreviewProps } from "../typings/NumberInputProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: NumberInputPreviewProps, schema: Properties): Properties {
    return schema;
}
