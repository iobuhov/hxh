import { DonutChartPreviewProps } from "../typings/DonutChartProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: DonutChartPreviewProps, schema: Properties): Properties {
    return schema;
}
