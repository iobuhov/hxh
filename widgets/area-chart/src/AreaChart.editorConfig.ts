import { AreaChartPreviewProps } from "../typings/AreaChartProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: AreaChartPreviewProps, schema: Properties): Properties {
    return schema;
}
