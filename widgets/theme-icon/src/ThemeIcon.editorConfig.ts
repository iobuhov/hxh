import { ThemeIconPreviewProps } from "../typings/ThemeIconProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: ThemeIconPreviewProps, schema: Properties): Properties {
    return schema;
}
