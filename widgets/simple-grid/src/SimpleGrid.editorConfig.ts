import { SimpleGridPreviewProps } from "../typings/SimpleGridProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: SimpleGridPreviewProps, schema: Properties): Properties {
    return schema;
}
