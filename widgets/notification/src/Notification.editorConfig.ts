import { NotificationPreviewProps } from "../typings/NotificationProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: NotificationPreviewProps, schema: Properties): Properties {
    return schema;
}
