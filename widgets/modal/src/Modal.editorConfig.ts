import { ModalPreviewProps } from "../typings/ModalProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: ModalPreviewProps, schema: Properties): Properties {
    return schema;
}
