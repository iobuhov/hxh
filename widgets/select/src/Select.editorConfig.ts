import { SelectPreviewProps } from "../typings/SelectProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: SelectPreviewProps, schema: Properties): Properties {
    return schema;
}
