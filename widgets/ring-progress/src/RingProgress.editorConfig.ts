import { RingProgressPreviewProps } from "../typings/RingProgressProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: RingProgressPreviewProps, schema: Properties): Properties {
    return schema;
}
