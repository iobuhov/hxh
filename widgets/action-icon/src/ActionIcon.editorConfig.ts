import { ActionIconPreviewProps } from "../typings/ActionIconProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: ActionIconPreviewProps, schema: Properties): Properties {
    return schema;
}
