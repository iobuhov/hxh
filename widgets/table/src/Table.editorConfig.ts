import { TablePreviewProps } from "../typings/TableProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: TablePreviewProps, schema: Properties): Properties {
    return schema;
}
