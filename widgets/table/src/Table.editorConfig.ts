import { TablePreviewProps } from "../typings/TableProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: TablePreviewProps, defaultProperties: Properties): Properties {
    return defaultProperties;
}
