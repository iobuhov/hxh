import { TreePreviewProps } from "../typings/TreeProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: TreePreviewProps, schema: Properties): Properties {
    return schema;
}
