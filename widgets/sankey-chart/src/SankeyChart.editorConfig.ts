import { SankeyChartPreviewProps } from "../typings/SankeyChartProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: SankeyChartPreviewProps, schema: Properties): Properties {
    return schema;
}
