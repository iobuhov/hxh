import { BubbleChartPreviewProps } from "../typings/BubbleChartProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: BubbleChartPreviewProps, schema: Properties): Properties {
    return schema;
}
