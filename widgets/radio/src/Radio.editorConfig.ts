import { RadioPreviewProps } from "../typings/RadioProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: RadioPreviewProps, schema: Properties): Properties {
    return schema;
}
