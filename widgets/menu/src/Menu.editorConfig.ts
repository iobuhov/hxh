import { MenuPreviewProps } from "../typings/MenuProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: MenuPreviewProps, schema: Properties): Properties {
    return schema;
}
