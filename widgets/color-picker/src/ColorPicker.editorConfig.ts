import { ColorPickerPreviewProps } from "../typings/ColorPickerProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: ColorPickerPreviewProps, schema: Properties): Properties {
    return schema;
}
