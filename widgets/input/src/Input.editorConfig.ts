import { InputPreviewProps } from "../typings/InputProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: InputPreviewProps, schema: Properties): Properties {
    return schema;
}
