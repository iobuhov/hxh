import { SpoilerPreviewProps } from "../typings/SpoilerProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: SpoilerPreviewProps, schema: Properties): Properties {
    return schema;
}
