import { TextInputPreviewProps } from "../typings/TextInputProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: TextInputPreviewProps, schema: Properties): Properties {
    return schema;
}
