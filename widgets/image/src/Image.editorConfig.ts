import { ImagePreviewProps } from "../typings/ImageProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: ImagePreviewProps, schema: Properties): Properties {
    return schema;
}
