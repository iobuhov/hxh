import { LoadingOverlayPreviewProps } from "../typings/LoadingOverlayProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: LoadingOverlayPreviewProps, schema: Properties): Properties {
    return schema;
}
