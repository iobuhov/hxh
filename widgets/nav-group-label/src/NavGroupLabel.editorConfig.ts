import { NavGroupLabelPreviewProps } from "../typings/NavGroupLabelProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: NavGroupLabelPreviewProps, defaultProperties: Properties): Properties {
    return defaultProperties;
}
