import { ThemeProviderPreviewProps } from "../typings/ThemeProviderProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: ThemeProviderPreviewProps, schema: Properties): Properties {
    return schema;
}
