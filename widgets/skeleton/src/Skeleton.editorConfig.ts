import { SkeletonPreviewProps } from "../typings/SkeletonProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: SkeletonPreviewProps, schema: Properties): Properties {
    return schema;
}
