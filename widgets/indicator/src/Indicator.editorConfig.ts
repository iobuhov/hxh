import { IndicatorPreviewProps } from "../typings/IndicatorProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: IndicatorPreviewProps, schema: Properties): Properties {
    return schema;
}
