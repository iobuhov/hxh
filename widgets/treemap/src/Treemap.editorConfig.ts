import { TreemapPreviewProps } from "../typings/TreemapProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: TreemapPreviewProps, schema: Properties): Properties {
    return schema;
}
