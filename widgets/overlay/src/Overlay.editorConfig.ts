import { OverlayPreviewProps } from "../typings/OverlayProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: OverlayPreviewProps, schema: Properties): Properties {
    return schema;
}
