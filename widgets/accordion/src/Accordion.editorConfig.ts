import { AccordionPreviewProps } from "../typings/AccordionProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: AccordionPreviewProps, schema: Properties): Properties {
    return schema;
}
