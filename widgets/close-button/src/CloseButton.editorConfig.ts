import { CloseButtonPreviewProps } from "../typings/CloseButtonProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: CloseButtonPreviewProps, schema: Properties): Properties {
    return schema;
}
