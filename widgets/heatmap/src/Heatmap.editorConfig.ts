import { HeatmapPreviewProps } from "../typings/HeatmapProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: HeatmapPreviewProps, schema: Properties): Properties {
    return schema;
}
