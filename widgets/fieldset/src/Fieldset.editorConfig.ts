import { FieldsetPreviewProps } from "../typings/FieldsetProps";
import { Properties } from "../typings/editor-types";

export function getProperties(_values: FieldsetPreviewProps, schema: Properties): Properties {
    return schema;
}
